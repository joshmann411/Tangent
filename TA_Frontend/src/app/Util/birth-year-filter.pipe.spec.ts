import { BirthYearFilterPipe } from './birth-year-filter.pipe';

describe('BirthYearFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new BirthYearFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
